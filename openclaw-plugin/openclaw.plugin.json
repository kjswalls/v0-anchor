{
  "id": "dsul-context",
  "kind": "plugin",
  "name": "dsul Context",
  "description": "Cache-backed dsul task/habit/project context injected into every OpenClaw prompt. Subscribes to dsul change webhooks for zero-latency freshness.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dsul-context": {
        "type": "object",
        "required": ["dsulUrl", "apiKey"],
        "properties": {
          "dsulUrl":        { "type": "string", "description": "Base URL of your dsul deployment (e.g. https://dsul.yourdomain.com)" },
          "apiKey":         { "type": "string", "description": "Your personal dsul API key — generate it in dsul → Settings → AI Assistant → OpenClaw" },
          "publicUrl":      { "type": "string", "description": "Your gateway's public URL (e.g. https://your-machine.ts.net) — registers …/plugins/dsul/chat with dsul for sidebar chat" },
          "agentId":        { "type": "string", "description": "OpenClaw agent id for dsul sidebar chat (default: main)" },
          "id":             { "type": "string", "description": "Optional alias for agent id if agentId is not set" },
          "webhookSecret":  { "type": "string", "description": "Optional HMAC secret for verifying change event payloads" },
          "cacheTtlMs":     { "type": "number", "description": "Max cache age before forcing a re-fetch (default: 300000ms = 5 min)" }
        }
      }
    }
  }
}
